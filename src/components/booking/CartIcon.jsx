
import React from 'react';

/**
 * DEPRECATED: Cart icon component for old booking system
 * Booking cart has been removed - all bookings via manual contact
 * Kept for backwards compatibility only - returns null
 */

const CartIcon = () => {
  return null;
};

export default CartIcon;
